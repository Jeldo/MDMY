mutation CreateMeeting($meetingName: String!,$numberOfParticipants: Int!){
    createMeeting(meetingName:$meetingName,numberOfParticipants:$numberOfParticipants){
        _id
        meetingName
        numberOfParticipants
        token
    }
}
