query getMeetingByToken(
    $token: String!
){
    getMeetingByToken(token: $token) {
        meetingName
        participants{
            location{
                coordinates
            }
        }
        numberOfParticipants
    }
}