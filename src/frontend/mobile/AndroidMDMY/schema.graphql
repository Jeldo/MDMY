# This file was generated based on ".graphqlconfig". Do not edit manually.

schema {
    query: Query
    mutation: Mutation
}

type Meeting {
    id: ID!
    meetingName: String!
    meetingPeopleNumber: Int!
    meetingUsers: [MeetingUser]
    token: String
}

type MeetingUser {
    id: ID!
    meetingId: String!
    userLocation: String!
    userName: String!
}

type Mutation {
    addMeeting(meetingName: String!, meetingPeopleNumber: Int!): Meeting
    addMeetingUser(token: String!, userLocation: String!, userName: String!): MeetingUser
    deleteMeeting(id: ID!): Meeting
    deleteMeetingUser(id: ID!): MeetingUser
}

type Query {
    getMeetingById(id: ID!): Meeting
    getMeetingByName(meetingName: String!): Meeting
    getMeetingUserById(id: ID!): MeetingUser
    getMeetingUsers: [MeetingUser]
    getMeetings: [Meeting]
}

enum CacheControlScope {
    PRIVATE
    PUBLIC
}


"The `Upload` scalar type represents a file upload."
scalar Upload